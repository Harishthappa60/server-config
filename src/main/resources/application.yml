server:
  port: 8882
spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
          url: https://github.com/Harishthappa60/server-config.git
          clone-on-start: true
          default-label: main
          
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8881/eureka

management:
  endpoints:
    web:
      exposure:
       include: health,info